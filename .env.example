# ── Copy this to Vercel → Project Settings → Environment Variables ──

# Gemini (YOUR key — users never see this)
GEMINI_API_KEY=AIzaSy...

# Supabase
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJ...   # Service role key (Settings → API → service_role)
SUPABASE_ANON_KEY=eyJ...      # Anon/public key (Settings → API → anon)

# Stripe
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...   # from Stripe Dashboard → Webhooks → signing secret
